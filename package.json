{
  "name": "login-with-metamask",
  "version": "0.1.0",
  "author": "Jacob Hsu <jacob.hsu.tw@gmail.com>",
  "description": "Demo project for 'One-click Login with Blockchain: A MetaMask Tutorial'",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna exec yarn build --parallel",
    "lint": "lerna exec yarn lint --parallel",
    "start": "lerna exec yarn start --parallel",
    "test": "lerna exec yarn test --parallel"
  },
  "devDependencies": {
    "@types/jest": "^24.0.23",
    "lerna": "^3.19.0",
    "prettier": "^1.19.1",
    "typescript": "^3.7.2"
  }
}
